[project]
name = "euro-rv-preview"
version = "0.1.0"
description = "Local preview server for the Euro-RV workshop site"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "markdown>=3.3",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
